[core]
    no_scm = True
    remote = local-minio
['remote "local-minio"']
    url = s3://dvc-storage
    endpointurl = http://minio_s3:9000
    access_key_id = mlops_user
    secret_access_key = mlops_password
    region = us-east-1
    ssl_verify = false
