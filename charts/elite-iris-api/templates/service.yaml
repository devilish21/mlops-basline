apiVersion: v1
kind: Service
metadata:
  name: {{ include "elite-iris-api.fullname" . }}
  labels:
    {{- include "elite-iris-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "elite-iris-api.selectorLabels" . | nindent 4 }}
